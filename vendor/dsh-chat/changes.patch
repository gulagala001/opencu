diff --git a/packages/client/tsdown.client.ts b/packages/client/tsdown.client.ts
index 99e647d2..d7786d87 100644
--- a/packages/client/tsdown.client.ts
+++ b/packages/client/tsdown.client.ts
@@ -558,7 +558,7 @@ function clientConfig(id: string, entry: string, clientBanner?: (fileName: strin
         this.addWatchFile(fileId)
         const source = await readFile(fileId)
         const { code, exports: cssExports } = transform({
-          filename: fileId,
+          filename: "/home/runner/work/deepseek-harness/deepseek-harness/" + relative(REPOSITORY_ROOT, fileId).split(sep).join("/"),
           code: source,
           cssModules: { pattern: '[hash]_[local]' },
           minify: true,
@@ -582,7 +582,7 @@ function clientConfig(id: string, entry: string, clientBanner?: (fileName: strin
         const fileId = virtualId.slice(INLINE_CSS_VIRTUAL_PREFIX.length, -CSS_VIRTUAL_SUFFIX.length)
         this.addWatchFile(fileId)
         const source = await readFile(fileId)
-        const { code } = transform({ filename: fileId, code: source, minify: true })
+        const { code } = transform({ filename: "/home/runner/work/deepseek-harness/deepseek-harness/" + relative(REPOSITORY_ROOT, fileId).split(sep).join("/"), code: source, minify: true })
         return `export default ${JSON.stringify(code.toString())};`
       },
     }, {
@@ -597,7 +597,7 @@ function clientConfig(id: string, entry: string, clientBanner?: (fileName: strin
         const fileId = virtualId.slice(GLOBAL_CSS_VIRTUAL_PREFIX.length, -CSS_VIRTUAL_SUFFIX.length)
         this.addWatchFile(fileId)
         const source = await readFile(fileId)
-        const { code } = transform({ filename: fileId, code: source, minify: true })
+        const { code } = transform({ filename: "/home/runner/work/deepseek-harness/deepseek-harness/" + relative(REPOSITORY_ROOT, fileId).split(sep).join("/"), code: source, minify: true })
         return styleInjectionModule(id, fileId, code.toString())
       },
     }],
diff --git a/packages/client/ui-chat/src/client/apply.ts b/packages/client/ui-chat/src/client/apply.ts
index 69885d3e..129c8874 100644
--- a/packages/client/ui-chat/src/client/apply.ts
+++ b/packages/client/ui-chat/src/client/apply.ts
@@ -1,3 +1,4 @@
+import type { ConfigForm } from '@deepseek-ai/dsh-client-ui-settings/client'
 /** Register the Chat Conversation target, renderers, stats, and details surface. */
 import type { Context } from '@deepseek-ai/cordis'
 import type { ImageAttachmentRef } from '@deepseek-ai/dsh-attachment'
@@ -62,7 +63,7 @@ export const inject = [
  * Mount all Chat-owned contributions.
  * @param ctx - Client root context.
  */
-export function apply(ctx: Context): void {
+export function apply(ctx: Context, options: { settings?: ConfigForm<ChatSettings> } = {}): void {
   const chatSources = new WeakMap<SessionBinding, ObservableSnapshot<ChatSnapshot>>()
   const chatSource = (binding: SessionBinding): ObservableSnapshot<ChatSnapshot> => {
     let source = chatSources.get(binding)
@@ -86,7 +87,7 @@ export function apply(ctx: Context): void {
   const t = ctx.locale.bind(NS)
   const chatStore = createChatStore()
   const chatScrollPositions = new Map<SessionId, ChatScrollPosition>()
-  const chatSettings = ctx.configForms.get<ChatSettings>(CHAT_SETTINGS_NAMESPACE)
+  const chatSettings = options.settings ?? ctx.configForms.get<ChatSettings>(CHAT_SETTINGS_NAMESPACE)
   const linkOpening = createSnapshotStore(chatSettings.getSnapshot().value?.linkOpening ?? DEFAULT_LINK_OPENING)
   ctx.effect(() => chatSettings.subscribe(() => {
     const accepted = chatSettings.getSnapshot().value?.linkOpening
diff --git a/packages/client/ui-chat/src/client/chat/AssistantMarkdown.tsx b/packages/client/ui-chat/src/client/chat/AssistantMarkdown.tsx
index 1dd235d3..7f4ec270 100644
--- a/packages/client/ui-chat/src/client/chat/AssistantMarkdown.tsx
+++ b/packages/client/ui-chat/src/client/chat/AssistantMarkdown.tsx
@@ -25,7 +25,7 @@ export function localPathMediaUrl(base: string, value: string): string | undefin
 export interface AssistantMarkdownProps {
   /** Render only the requested business portion, preserving original block indexes. */
   groupPart?: string | undefined
-  /** Stable Hook forwarded to each independently expandable reasoning block. */
+  /** Stable Hook forwarded to each independently expandable run of reasoning blocks. */
   useDisclosure: UseDisclosure
   blocks: readonly AssistantBlock[]
   streaming: boolean
@@ -45,7 +45,7 @@ export interface AssistantMarkdownProps {
   t: ChatViewSlotProps['t']
 }
 
-/** Reasoning block as the Think variant summary row (figma 39:28304). */
+/** Adjacent reasoning blocks in one Assistant share a Think disclosure. */
 export const AssistantMarkdown = memo(function AssistantMarkdown({
   blocks, streaming, interrupted, renderMessageImages, groupPart, useDisclosure,
   reasoningHidden = false, usePresentation, revealProcess, mentions, t,
@@ -84,18 +84,27 @@ export const AssistantMarkdown = memo(function AssistantMarkdown({
           />,
         )
         break
-      case 'reasoning':
+      case 'reasoning': {
+        const start = i
+        const parts = [block.text]
+        while (i + 1 < blocks.length) {
+          const next = blocks[i + 1]
+          if (next?.kind !== 'reasoning') break
+          parts.push(next.text)
+          i += 1
+        }
         rendered.push(
           <ProcessReasoning
-            key={i}
+            key={start}
             hidden={reasoningHidden}
             reveal={revealProcess}
           >
-            <ReasoningRow text={block.text} running={streaming && i === last} usePresentation={usePresentation}
+            <ReasoningRow text={parts.join('\n\n')} running={streaming && i === last} usePresentation={usePresentation}
               useDisclosure={useDisclosure} t={t} />
           </ProcessReasoning>,
         )
         break
+      }
       case 'image': {
         // Consecutive image blocks share one gallery so several images tile
         // into rows instead of each opening a one-image group of its own.
diff --git a/packages/client/ui-chat/src/client/chat/ChatGroupSeat.module.css b/packages/client/ui-chat/src/client/chat/ChatGroupSeat.module.css
index 56687cd8..d01394bb 100644
--- a/packages/client/ui-chat/src/client/chat/ChatGroupSeat.module.css
+++ b/packages/client/ui-chat/src/client/chat/ChatGroupSeat.module.css
@@ -72,8 +72,9 @@
 }
 
 .body {
-  --dsh-chat-flow-gap: 8px;
-  max-height: min(400px, 50vh);
+  --dsh-chat-flow-gap: 2px;
+  position: relative;
+  max-height: min(280px, 45vh);
   overflow-y: auto;
   overscroll-behavior-y: auto;
   scrollbar-gutter: stable;
@@ -126,3 +127,5 @@
   overflow: visible;
   scrollbar-gutter: auto;
 }
+
+.count { flex: none; font-size: 11px; color: var(--dsw-alias-label-tertiary); }
diff --git a/packages/client/ui-chat/src/client/chat/ChatGroupSeat.tsx b/packages/client/ui-chat/src/client/chat/ChatGroupSeat.tsx
index f2d7fa17..fef2d281 100644
--- a/packages/client/ui-chat/src/client/chat/ChatGroupSeat.tsx
+++ b/packages/client/ui-chat/src/client/chat/ChatGroupSeat.tsx
@@ -1,5 +1,5 @@
 /** Stable process container; display policy changes visibility, never member parents. */
-import { memo, useCallback, useEffect, useId, useRef, useState, type ComponentProps, type ReactNode } from 'react'
+import { memo, useCallback, useEffect, useLayoutEffect, useId, useRef, useState, type ComponentProps, type ReactNode } from 'react'
 import {
   IconAgentPresetOutlineRegular, IconApiOutlineRegular, IconBrowseOutlineRegular, IconChevronDownOutlineRegular,
   IconChevronUpOutlineRegular, IconCodeOutlineRegular, IconEditOutlineRegular, IconGlobeOutlineRegular,
@@ -34,6 +34,8 @@ interface LiveProcessTitle {
 }
 
 const PROCESS_ICONS: Record<ProcessTitleActivity, ReactNode> = {
+  images: <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.6"><path d="M3 3h18v18H3zM3 17l6-7 5 5 3-3 4 5"/><circle cx="16" cy="7" r="1"/></svg>,
+  computer: <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.6"><path d="M3 4h18v13H3zM8 21h8M12 17v4" /></svg>,
   thinking: <IconThinkOutlineRegular />,
   read: <IconBrowseOutlineRegular size={14} />,
   search: <IconSearchOutlineRegular size={14} />,
@@ -95,18 +97,19 @@ const ProcessGroupHeader = memo(function ProcessGroupHeader({ groupKey, useChatG
   readonly toggle: () => void
 }) {
   const data = useChatGroup(groupKey, group => group?.data)
-  const detailed = usePresentation(policy => data?.closed === false && policy.liveProcessDetail)
+  const working = data !== undefined && !data.closed && (data.summary.running !== undefined || data.summary.counts.length === 0)
+  const detailed = usePresentation(policy => working && policy.liveProcessDetail)
   const live = useStableLiveProcessTitle({
     activity: data?.summary.running ?? 'thinking',
     detail: data?.summary.runningDetail ?? '',
-  }, data !== undefined && !data.closed)
+  }, working)
   if (data === undefined) return null
-  const label = data.closed ? processTitle(data.summary, t) : t(`message.stepProcess.${live.activity}`)
-  const detail = detailed && !data.closed ? live.detail : ''
+  const label = working ? t(`message.stepProcess.${live.activity}`) : processTitle(data.summary, t)
+  const detail = detailed && working ? live.detail : ''
   const title = detail === '' ? label : `${label}${t('message.turnProcess.separator')}${detail}`
-  const activity = data.closed ? data.summary.counts[0]?.kind ?? 'thinking' : live.activity
+  const activity = working ? live.activity : data.summary.counts[0]?.kind ?? 'thinking'
   return (
-    <button type="button" className={css.title} aria-expanded={open} aria-controls={bodyId}
+    <button type="button" id={bodyId + '-title'} className={`${css.title} tx-cu-group-toggle`} aria-expanded={open} aria-controls={bodyId}
       data-process-activity={activity} onClick={(event) => { event.currentTarget.focus(); toggle() }}>
       <span className={css.leading} aria-hidden="true">
         <span className={css.activityIcon} data-step-process-icon>{PROCESS_ICONS[activity]}</span>
@@ -114,7 +117,10 @@ const ProcessGroupHeader = memo(function ProcessGroupHeader({ groupKey, useChatG
           {open ? <IconChevronUpOutlineRegular /> : <IconChevronDownOutlineRegular />}
         </span>
       </span>
-      <TextShimmer active={!data.closed} className={css.label}>{title}</TextShimmer>
+      <TextShimmer active={working} className={css.label}>{title}</TextShimmer>
+      {data.summary.counts.length > 0 && <span className={css.count} data-process-count>{t('message.stepProcess.count', { count: data.summary.counts.reduce((total, item) => total + item.count, 0) })}</span>}
+      {(data.summary.failures ?? 0) > 0 && <span className="tx-cu-error" data-process-failures>{t('message.stepProcess.failures', { count: data.summary.failures ?? 0 })}</span>}
+      {(data.summary.stopped ?? 0) > 0 && <span data-process-stopped>{t('message.stepProcess.stopped', { count: data.summary.stopped ?? 0 })}</span>}
     </button>
   )
 })
@@ -146,9 +152,11 @@ export const ChatGroupSeat = memo(function ChatGroupSeat({ groupKey, useChatGrou
     if (spec !== undefined && !alwaysOpen) props.actions.setTurnProcessOpen(spec.turn, spec.answerStep ?? 0, true)
   }, [props.actions, spec, alwaysOpen])
   const rootRef = useSearchableHidden(outerHidden, revealOuter)
-  useEffect(() => {
-    if (outerHidden && rootRef.current?.hasAttribute('hidden')) setOpen(false)
-  }, [outerHidden, rootRef, setOpen])
+  const wasClosed = useRef(presentation?.turnClosed)
+  useLayoutEffect(() => {
+    if (presentation?.turnClosed && !wasClosed.current && open) revealOuter()
+    wasClosed.current = presentation?.turnClosed
+  }, [presentation?.turnClosed, open, revealOuter])
   const reveal = useCallback(() => { setOpen(true) }, [setOpen])
   const bodyRef = useSearchableHidden(grouped && !open, reveal)
   const contentRef = useRef<HTMLDivElement>(null)
@@ -162,7 +170,7 @@ export const ChatGroupSeat = memo(function ChatGroupSeat({ groupKey, useChatGrou
   const classes = [css.body, !grouped ? css.expandedBody : '',
     grouped && edges.canScrollUp ? css.fadeTop : '', grouped && edges.canScrollDown ? css.fadeBottom : '']
   return (
-    <div ref={rootRef} className={css.root} data-chat-group-key={groupKey}
+    <div ref={rootRef} className={`${css.root} tx-cu-group`} data-chat-group-key={groupKey} data-cu-group={groupKey}
       data-chat-flow-key={groupKey} data-chat-anchor-key={`group:${groupKey}`} data-chat-turn={turn}
       data-chat-paging-anchor={grouped && !open || undefined}
       data-step-process data-group-expanded-mode={!grouped || undefined}>
@@ -170,7 +178,7 @@ export const ChatGroupSeat = memo(function ChatGroupSeat({ groupKey, useChatGrou
         <ProcessGroupHeader groupKey={groupKey} useChatGroup={useChatGroup}
           usePresentation={props.usePresentation} t={props.t} open={open} bodyId={bodyId} toggle={toggle} />
       </div>
-      <div ref={bodyRef} id={bodyId} className={classes.join(' ')} data-step-process-body
+      <div ref={bodyRef} id={bodyId} role="region" tabIndex={0} aria-labelledby={bodyId + '-title'} className={classes.join(' ')} data-step-process-body
         data-scroll-up={edges.canScrollUp || undefined} data-scroll-down={edges.canScrollDown || undefined}
         {...events}>
         <div ref={contentRef} className={css.content} data-step-process-content data-chat-flow="">
diff --git a/packages/client/ui-chat/src/client/chat/ChatNodeSeat.tsx b/packages/client/ui-chat/src/client/chat/ChatNodeSeat.tsx
index 5bb003bd..7b1b62ca 100644
--- a/packages/client/ui-chat/src/client/chat/ChatNodeSeat.tsx
+++ b/packages/client/ui-chat/src/client/chat/ChatNodeSeat.tsx
@@ -1,4 +1,4 @@
-import { memo, useCallback, useEffect, useMemo, useState } from 'react'
+import { memo, useCallback, useMemo, useState } from 'react'
 import { JsonBlock } from '@deepseek-ai/dsh-client-ui-primitives'
 import { createSnapshotStore } from '@deepseek-ai/dsh-client-store'
 import type { ConversationLocationDataStore, ConversationTurnDataMap } from '@deepseek-ai/dsh-client-ui-conversation/client'
@@ -115,11 +115,7 @@ export const ChatNodeSeat = memo(function ChatNodeSeat({
   const [disclosureReset] = useState(() => createSnapshotStore(0))
   const turnData = turnDataOf(routedNode)
   const hookContext = useMemo<ChatNodeHookContext>(() => ({ turnData, disclosureReset }), [turnData, disclosureReset])
-  useEffect(() => {
-    if (processMember && processHidden && wrapperRef.current?.hasAttribute('hidden')) {
-      disclosureReset.set(disclosureReset.getSnapshot() + 1)
-    }
-  }, [processMember, processHidden, wrapperRef, disclosureReset])
+  // Parent folds change visibility only; preserve each detail's reading state.
   const owner = useMemo<ChatNodeOwnerProps | null>(() => node === undefined
     ? null
     : {
diff --git a/packages/client/ui-chat/src/client/chat/ReasoningRow.tsx b/packages/client/ui-chat/src/client/chat/ReasoningRow.tsx
index 74a801d6..95a4d1ae 100644
--- a/packages/client/ui-chat/src/client/chat/ReasoningRow.tsx
+++ b/packages/client/ui-chat/src/client/chat/ReasoningRow.tsx
@@ -32,7 +32,7 @@ function latestCompletedParagraphFirstLine(text: string): string {
 }
 
 /**
- * Render one assistant reasoning block collapsed until the reader opens it. The
+ * Render one consecutive run of assistant reasoning collapsed until the reader opens it. The
  * collapsed summary omits double-asterisk markers; expanded content renders
  * the complete Markdown with secondary typography. A streaming preview advances
  * when a paragraph's first line completes. Mode changes toggle CSS display without unmounting
diff --git a/packages/client/ui-chat/src/client/contract/process-groups.ts b/packages/client/ui-chat/src/client/contract/process-groups.ts
index 0c7bc744..0390064e 100644
--- a/packages/client/ui-chat/src/client/contract/process-groups.ts
+++ b/packages/client/ui-chat/src/client/contract/process-groups.ts
@@ -1,9 +1,11 @@
 /** Business data for Chat process groups; source Nodes remain in the Chat store. */
 export type ProcessActivity = 'read' | 'search' | 'edit' | 'commands' | 'code'
-  | 'webSearch' | 'webFetch' | 'subagents' | 'plan' | 'questions' | 'tools'
+  | 'webSearch' | 'webFetch' | 'subagents' | 'plan' | 'questions' | 'tools' | 'computer' | 'images'
 
 /** Distinct-call category ranking and the current live task detail. */
 export interface ProcessActivitySummary {
+  readonly failures?: number
+  readonly stopped?: number
   readonly counts: readonly { readonly kind: ProcessActivity; readonly count: number }[]
   readonly running: ProcessActivity | undefined
   readonly runningDetail: string
diff --git a/packages/client/ui-chat/src/client/conversation-nodes/README.md b/packages/client/ui-chat/src/client/conversation-nodes/README.md
index 3a166067..022dea0c 100644
--- a/packages/client/ui-chat/src/client/conversation-nodes/README.md
+++ b/packages/client/ui-chat/src/client/conversation-nodes/README.md
@@ -155,7 +155,7 @@ Group headers show a category icon, replace it with a down arrow on hover or key
 
 An open capped group follows content growth only while its own scroll position is at the bottom. Scrolling away pauses that group's following; returning to the bottom resumes it, independently of outer transcript following. Manually opening an unclosed group starts at the bottom and follows growth; manually opening a closed group starts at the top with following disabled, even when its initial content fits without scrolling. Closing the group in the data or restoring its height cap through a mode change does not reset an already-open reader's position. Browser find retains its own reveal position.
 
-Individual reasoning starts collapsed, including while streaming. All modes preview the latest paragraph whose first line ends with a newline; an unfinished single line has no preview. Later text in that paragraph does not change the preview. After settlement, the mode table applies. Expanded reasoning uses compact Markdown typography.
+Consecutive reasoning blocks within one Assistant share a single disclosure, preserving their text in order and the reader’s open state as fragments arrive. Other block kinds and Assistant steps separate disclosures. Individual reasoning starts collapsed, including while streaming. All modes preview the latest paragraph whose first line ends with a newline; an unfinished single line has no preview. Later text in that paragraph does not change the preview. After settlement, the mode table applies. Expanded reasoning uses compact Markdown typography.
 
 Switching modes retains manually opened groups and inner disclosures. It changes visibility and sizing without recreating the message rows. Expanded does not mean opening every individual disclosure or unfolding the whole Turn.
 
diff --git a/packages/client/ui-chat/src/client/conversation-nodes/README.zh.md b/packages/client/ui-chat/src/client/conversation-nodes/README.zh.md
index 024ea0f5..61c1f09d 100644
--- a/packages/client/ui-chat/src/client/conversation-nodes/README.zh.md
+++ b/packages/client/ui-chat/src/client/conversation-nodes/README.zh.md
@@ -155,7 +155,7 @@ description: "Chat 过程分组与活动摘要的明细业务规则。"
 
 已展开的限高组仅在自己的滚动位置处于底部时跟随内容增长。滚离底部暂停该组跟随，滚回底部后恢复，与外层文本记录的跟随状态独立。手动展开未关闭的组时定位到底部并跟随增长；手动展开已关闭的组时从顶部开始且不启用跟随，即使初始内容无需滚动即可容纳。数据中的组变为已关闭，或通过模式切换恢复限高时，不重置已展开组的阅读位置。浏览器查找沿用自身的展开定位。
 
-单个推理初始收起，包括流式输出期间。三档都预览最近一个首行以换行结束的段落；未完成的单行没有预览，该段落后续文本不改变预览。结算后按上表展示。展开的推理使用紧凑 Markdown 排版。
+同一 Assistant 内连续的推理块合用一个折叠行，按顺序保留全文，后续片段到达时保持用户的展开状态。其他类型的块及不同 Assistant 步骤仍分开显示。单个推理初始收起，包括流式输出期间。三档都预览最近一个首行以换行结束的段落；未完成的单行没有预览，该段落后续文本不改变预览。结算后按上表展示。展开的推理使用紧凑 Markdown 排版。
 
 切换模式保留手动展开的组和内部开合状态，只改变显隐与尺寸，不重建消息行。完全展开不表示自动打开每个内部控件，也不表示展开整轮。
 
diff --git a/packages/client/ui-chat/src/client/conversation-nodes/process-activity.ts b/packages/client/ui-chat/src/client/conversation-nodes/process-activity.ts
index d6766b19..add41fbd 100644
--- a/packages/client/ui-chat/src/client/conversation-nodes/process-activity.ts
+++ b/packages/client/ui-chat/src/client/conversation-nodes/process-activity.ts
@@ -5,7 +5,9 @@ import { isRunningTool } from '../contract/chat-nodes.ts'
 import type { ToolCallBlock } from '../contract/snapshot.ts'
 
 function activity(name: string): ProcessActivity {
-  if (['read', 'read_image', 'list_mcp_resources', 'list_mcp_resource_templates', 'read_mcp_resource'].includes(name)) return 'read'
+  if (/^computer_use(?:_|$)/.test(name.split(/[./]/).at(-1) ?? '')) return 'computer'
+  if (name === 'read_image') return 'images'
+  if (['read', 'list_mcp_resources', 'list_mcp_resource_templates', 'read_mcp_resource'].includes(name)) return 'read'
   if (name === 'grep' || name === 'glob' || name.endsWith('_inspect')) return 'search'
   if (['write', 'edit', 'apply_patch'].includes(name)) return 'edit'
   if (['bash', 'pwsh', 'exec_command', 'write_stdin'].includes(name) || name.startsWith('terminal_')) return 'commands'
@@ -95,9 +97,16 @@ export function processActivity(nodes: readonly ChatNode[]): ProcessActivitySumm
   let running: ProcessActivity | undefined
   let runningDetail = ''
   let runningTime = -Infinity
+  let failures = 0, stopped = 0
   const visit = (tool: ToolCallBlock): void => {
     if (seen.has(tool.callId)) return
     seen.add(tool.callId)
+    if (!isRunningTool(tool)) {
+      const name = tool.call?.name ?? ''
+      const stoppedComputer = activity(name) === 'computer' && tool.isError && /tool call aborted|Computer Use (?:was |is )?stopped/i.test(tool.content.filter(block => block.type === 'text').map(block => block.text).join('\n'))
+      if (stoppedComputer || ['ABORTED', 'ABORTED_BEFORE_DISPATCH', 'interrupted', 'COMPUTER_USE_STOPPED'].includes(tool.error?.code ?? '')) stopped++
+      else if (tool.isError || tool.meta && Reflect.get(tool.meta, 'computerUseError')) failures++
+    }
     const call = isRunningTool(tool) ? tool : tool.call
     if (call !== null) {
       const kind = activity(call.name)
@@ -115,6 +124,8 @@ export function processActivity(nodes: readonly ChatNode[]): ProcessActivitySumm
   }
   if (running === undefined) runningDetail = liveReasoningDetail(nodes)
   return {
+    ...(failures > 0 ? { failures } : {}),
+    ...(stopped > 0 ? { stopped } : {}),
     counts: [...counts].map(([kind, count]) => ({ kind, count })).sort((a, b) => b.count - a.count),
     running,
     runningDetail,
diff --git a/packages/client/ui-chat/src/client/conversation-nodes/process-groups.ts b/packages/client/ui-chat/src/client/conversation-nodes/process-groups.ts
index bff0c532..160edaff 100644
--- a/packages/client/ui-chat/src/client/conversation-nodes/process-groups.ts
+++ b/packages/client/ui-chat/src/client/conversation-nodes/process-groups.ts
@@ -29,6 +29,7 @@ function reply(node: ChatNode): boolean {
 
 function sameSummary(left: ProcessActivitySummary, right: ProcessActivitySummary): boolean {
   return left.running === right.running && left.runningDetail === right.runningDetail
+    && left.failures === right.failures && left.stopped === right.stopped
     && left.counts.length === right.counts.length && left.counts.every((value, index) =>
     value.kind === right.counts[index]?.kind && value.count === right.counts[index].count)
 }
diff --git a/packages/client/ui-chat/src/client/locale.ts b/packages/client/ui-chat/src/client/locale.ts
index 9638f554..6147fd52 100644
--- a/packages/client/ui-chat/src/client/locale.ts
+++ b/packages/client/ui-chat/src/client/locale.ts
@@ -17,11 +17,18 @@ export const zh = {
   'message.stepProcess.plan': '正在更新计划',
   'message.stepProcess.questions': '等待你的操作',
   'message.stepProcess.tools': '正在调用工具',
+  'message.stepProcess.computer': '正在操作电脑',
+  'message.stepProcess.done.computer': '已操作电脑',
+  'message.stepProcess.images': '正在查看图像',
+  'message.stepProcess.done.images': '已查看图像',
+  'message.stepProcess.count': '{count} 次操作',
+  'message.stepProcess.failures': '{count} 项失败',
+  'message.stepProcess.stopped': '{count} 项已停止',
   'message.stepProcess.done.thinking': '已完成分析',
   'message.stepProcess.done.read': '已读取文件',
   'message.stepProcess.done.search': '已搜索代码',
   'message.stepProcess.done.edit': '修改了文件',
-  'message.stepProcess.done.commands': '执行了命令',
+  'message.stepProcess.done.commands': '已运行命令',
   'message.stepProcess.done.code': '运行了代码',
   'message.stepProcess.done.webSearch': '已搜索网页',
   'message.stepProcess.done.webFetch': '已访问网页',
@@ -175,6 +182,13 @@ export const en = {
   'message.stepProcess.plan': 'Updating the plan',
   'message.stepProcess.questions': 'Waiting for your action',
   'message.stepProcess.tools': 'Calling tools',
+  'message.stepProcess.computer': 'Operating computer',
+  'message.stepProcess.done.computer': 'Operated computer',
+  'message.stepProcess.images': 'Viewing images',
+  'message.stepProcess.done.images': 'Viewed images',
+  'message.stepProcess.count': '{count} operations',
+  'message.stepProcess.failures': '{count} failed',
+  'message.stepProcess.stopped': '{count} stopped',
   'message.stepProcess.done.thinking': 'Analysis completed',
   'message.stepProcess.done.read': 'Read files',
   'message.stepProcess.done.search': 'Searched code',
diff --git a/packages/client/ui-chat/tests/reasoning-row.client.spec.tsx b/packages/client/ui-chat/tests/reasoning-row.client.spec.tsx
index 0d6b505f..6241b919 100644
--- a/packages/client/ui-chat/tests/reasoning-row.client.spec.tsx
+++ b/packages/client/ui-chat/tests/reasoning-row.client.spec.tsx
@@ -19,6 +19,51 @@ afterEach(() => {
 const t = makeTranslate(zh, commonZh)
 const renderMessageImages: AssistantMarkdownProps['renderMessageImages'] = () => null
 
+describe('consecutive reasoning fragments', () => {
+  const parts = ['First fragment\n', 'Second fragment\n', 'Third fragment\n', 'Fourth fragment\n']
+  const reasoning = parts.map(text => ({ kind: 'reasoning' as const, text }))
+  const tool = { kind: 'tool-call' as const, callId: 'call-1', name: 'read', argsRaw: '{}' }
+  const props = { useDisclosure, usePresentation: useDetailedPresentation, t, renderMessageImages }
+
+  it('keeps one open disclosure while fragments stream, settle, and replay', () => {
+    const view = render(<AssistantMarkdown {...props} blocks={reasoning.slice(0, 1)} streaming />)
+    const toggle = view.getByRole('button')
+    fireEvent.click(toggle)
+    for (let count = 2; count <= reasoning.length; count++) {
+      view.rerender(<AssistantMarkdown {...props} blocks={reasoning.slice(0, count)} streaming />)
+      expect(view.getAllByText('思考')).toHaveLength(1)
+      expect(view.getByRole('button')).toBe(toggle)
+      expect(toggle.getAttribute('aria-expanded')).toBe('true')
+      expect(view.container.querySelector('[data-variant="think"]')?.getAttribute('data-state')).toBe('running')
+      expect(view.container.querySelectorAll('[data-markdown-variant="compact"] p')).toHaveLength(count)
+    }
+    view.rerender(<AssistantMarkdown {...props} blocks={[...reasoning, tool]} streaming />)
+    expect(view.queryByText('运行中')).toBeNull()
+    view.rerender(<AssistantMarkdown {...props} blocks={[...reasoning, tool]} streaming={false} />)
+    expect(view.getByRole('button')).toBe(toggle)
+    expect(toggle.getAttribute('aria-expanded')).toBe('true')
+    expect(Array.from(view.container.querySelectorAll('[data-markdown-variant="compact"] p'), p => p.textContent))
+      .toEqual(parts.map(text => text.trim()))
+    view.unmount()
+
+    const replay = render(<AssistantMarkdown {...props} blocks={[...reasoning, tool]} streaming={false} />)
+    expect(replay.getAllByText('思考')).toHaveLength(1)
+    expect(replay.getByRole('button').getAttribute('aria-expanded')).toBe('false')
+    fireEvent.click(replay.getByRole('button'))
+    expect(Array.from(replay.container.querySelectorAll('[data-markdown-variant="compact"] p'), p => p.textContent))
+      .toEqual(parts.map(text => text.trim()))
+  })
+
+  it('keeps runs separated by prose or a tool call even in the reasoning-only view', () => {
+    const view = render(<AssistantMarkdown {...props} groupPart="reasoning" streaming={false}
+      blocks={[...reasoning, { kind: 'text', text: 'Progress update' }, ...reasoning, tool, ...reasoning]} />)
+    expect(view.getAllByText('思考')).toHaveLength(3)
+    expect(view.queryByText('Progress update')).toBeNull()
+    for (const toggle of view.getAllByRole('button')) fireEvent.click(toggle)
+    expect(view.container.querySelectorAll('[data-markdown-variant="compact"] p')).toHaveLength(12)
+  })
+})
+
 describe('ReasoningRow', () => {
   it.each([
     ['', ''],

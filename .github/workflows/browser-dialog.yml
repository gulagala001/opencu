name: Browser dialog regression
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  browser-dialog:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 12
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            pattern: 'extension browser dialogs'
          - os: windows-latest
            pattern: 'managed browser dialogs'
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-dotnet@v6
        if: runner.os == 'Windows'
        with:
          dotnet-version: '10.0.x'
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec playwright install chromium
      - run: pnpm build
      - run: node --test --test-name-pattern='${{ matrix.pattern }}' test/browser-dialog-ui.test.mjs
